# Copy to .env and adjust. All values are optional.

# Which NLP backend to use: rule (offline, deterministic) or llm.
EXTRACTOR=rule

# Required only when EXTRACTOR=llm.
# ANTHROPIC_API_KEY=sk-ant-...
LLM_MODEL=claude-sonnet-5

# Server behaviour
MAX_PROMPT_CHARS=4000
CORS_ORIGINS=*
DEBUG=false
