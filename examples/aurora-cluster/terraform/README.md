# aurora-postgre-cfa0ec

Terraform generated by the **AI-Driven Infrastructure Diagram and IaC Generator** from this requirement:

> An Aurora PostgreSQL cluster with an auto scaling group of web servers in private subnets behind a load balancer, highly available in production.

- Provider: `aws`
- Region: `us-east-1`
- Environment: `prod`
- Availability zones: 3

## Resources

- **Application Load Balancer** (`alb`) - explicit
- **Auto Scaling Group** (`app_asg`) - explicit
- **Aurora Cluster** (`app_db_cluster`) - explicit
- **Private Subnet** x3 (`private`) - explicit
- **VPC** (`main`) - implied
- **Public Subnet** x3 (`public`) - implied
- **ALB Security Group** (`alb_sg`) - implied
- **Target Group** (`app_tg`) - implied
- **App Security Group** (`app_sg`) - implied
- **EC2 Instance Role** (`instance_role`) - implied
- **Database Security Group** (`db_sg`) - implied
- **Route Table** (`private_rt`) - implied
- **NAT Gateway** x3 (`nat`) - implied
- **Internet Gateway** (`igw`) - implied
- **Route Table** (`public_rt`) - implied
- **Elastic IP** x3 (`eip`) - implied

## Deploy

```bash
terraform init
terraform plan
terraform apply
```

Review `terraform.tfvars` first. Nothing here has been applied to a real account -- always read the plan before approving it.

## Assumptions made during generation

- No region stated; defaulting to us-east-1.
- The EC2 instances described are the auto scaling group's members, so they are generated as a launch template rather than standalone hosts.
- VPC: A load balancer must live in a VPC.
- Public Subnet: An internet-facing load balancer needs public subnets.
- Security Group: The load balancer requires a security group.
- Target Group: A load balancer listener must forward to a target group.
- Security Group: Auto Scaling Group requires a security group.
- IAM Role: Scaled instances need an instance profile role.
- Security Group: The database cluster requires a security group.
- Route Table: The private subnet needs its own route table.
- NAT Gateway: Compute in the private subnet needs outbound internet access.
- Internet Gateway: A public subnet is only public with an internet gateway.
- Route Table: The public subnet needs a route table with a default route.
- Elastic IP: A NAT gateway requires an Elastic IP.
- One NAT gateway per availability zone (3 total) because high availability was requested.
