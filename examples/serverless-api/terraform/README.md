# serverless-rest-api-dev

Terraform generated by the **AI-Driven Infrastructure Diagram and IaC Generator** from this requirement:

> A serverless REST API: API Gateway in front of a Python Lambda function that reads and writes a DynamoDB table, with an SQS queue for background jobs. Development environment in ap-south-1.

- Provider: `aws`
- Region: `ap-south-1`
- Environment: `dev`
- Availability zones: 2

## Resources

- **Lambda Function** (`lambda_fn`) - explicit
- **API Gateway** (`api_gateway`) - explicit
- **DynamoDB Table** (`app_table`) - explicit
- **SQS Queue** (`work_queue`) - explicit
- **Lambda Execution Role** (`lambda_role`) - implied

## Deploy

```bash
terraform init
terraform plan
terraform apply
```

Review `terraform.tfvars` first. Nothing here has been applied to a real account -- always read the plan before approving it.

## Assumptions made during generation

- IAM Role: Lambda requires an execution role.
