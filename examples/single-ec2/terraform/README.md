# development-env-2c4973

Terraform generated by the **AI-Driven Infrastructure Diagram and IaC Generator** from this requirement:

> Create a development environment in us-east-1 with one Ubuntu EC2 instance inside a VPC. Add an Internet Gateway, Security Group allowing SSH and HTTP, IAM role, and Elastic IP.

- Provider: `aws`
- Region: `us-east-1`
- Environment: `dev`
- Availability zones: 2

## Resources

- **Internet Gateway** (`internet_gateway`) - explicit
- **EC2 Instance** (`app_server`) - explicit
- **VPC** (`main`) - explicit
- **App Security Group** (`app_sg`) - explicit
- **Elastic IP** (`eip`) - explicit
- **EC2 Instance Role** (`instance_role`) - explicit
- **Public Subnet** x2 (`public`) - implied
- **Route Table** (`public_rt`) - implied

## Deploy

```bash
terraform init
terraform plan
terraform apply
```

Review `terraform.tfvars` first. Nothing here has been applied to a real account -- always read the plan before approving it.

## Assumptions made during generation

- Public Subnet: EC2 Instance needs a subnet to launch into; public was used because no private subnet was requested.
- Route Table: The public subnet needs a route table with a default route.
