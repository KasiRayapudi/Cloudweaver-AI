# Runtime dependencies only.
#
# Vercel installs this file when building the serverless function, so it is
# deliberately minimal -- test and optional packages live in
# requirements-dev.txt and would otherwise be shipped into the deployment.

fastapi==0.115.6
pydantic==2.10.4
uvicorn==0.34.0
